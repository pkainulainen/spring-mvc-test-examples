TodoApp.Views.NotAuthorizedView = Marionette.ItemView.extend({
    template: "#template-not-authorized-view"
})

TodoApp.Views.NotFoundView = Marionette.ItemView.extend({
    template: "#template-not-found-view"
});

TodoApp.Views.ErrorView = Marionette.ItemView.extend({
   template: "#template-error-view"
});